|| Aantal functiedefinities voor de werkplaats.

gem a b = (a + b) // 2

verhoog n = n + 1

ing a b = a * b + a // b

cirkelOppervlak r = pi * r^2

opbrengst b r n = b*(1 + r)^n

f x = 1, if x = 1
    = 2, if x = 2

alleGelijk x y z = x = y /\ y = z 

alleVerschillend x y z = x ~= y /\ y ~= z

hoeveelGelijk x y z = 3, if alleGelijk x y z
                    = 1, if alleVerschillend x y z
                    = 2, otherwise

derde xs x y = drop x (take y xs)

plak3 xs ys zs = xs ++ ys ++ zs